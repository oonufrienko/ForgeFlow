import { Shell } from "@/components/shell";
import { Feedback } from "@/components/feedback";
import { createSupplierAction, updateMaterialAction } from "@/lib/actions";
import { requireSession } from "@/lib/auth/session";
import { readDatabase } from "@/lib/repository";

export default async function Inventory({ searchParams }: { searchParams: Promise<{ success?: string; error?: string }> }) { const [data, user] = await Promise.all([readDatabase(), requireSession()]); return <Shell eyebrow="Stock control" title="Inventory & suppliers"><Feedback searchParams={searchParams}/><section className="panel"><div className="panel-head"><div><p className="eyebrow">Raw materials</p><h2>Warehouse stock</h2></div></div><div className="table-wrap"><table><thead><tr><th>Item</th><th>On hand</th><th>Unit value</th><th>Stock state</th>{user.role === "Admin" && <th>Admin settings</th>}</tr></thead><tbody>{data.rawMaterials.map((item) => <tr key={item.materialId}><td><strong>{item.materialName}</strong><small>{item.sku}</small></td><td>{item.quantityInStock} {item.unitOfMeasure}</td><td>${item.unitPrice.toFixed(2)}</td><td><span className="badge neutral">{item.stockType.replace("_", " ")}</span></td>{user.role === "Admin" && <td><form action={updateMaterialAction} className="inline-form"><input type="hidden" name="materialId" value={item.materialId}/><input aria-label={`Reorder level for ${item.materialName}`} name="reorderLevel" type="number" min="0" step="0.01" defaultValue={item.reorderLevel}/><select aria-label={`Stock state for ${item.materialName}`} name="stockType" defaultValue={item.stockType}><option value="unrestricted">Unrestricted</option><option value="quality_inspection">Quality inspection</option><option value="blocked">Blocked</option></select><button className="button small">Save</button></form></td>}</tr>)}</tbody></table></div></section><section className="split equal"><div className="panel"><p className="eyebrow">Vendor network</p><h2>Suppliers</h2>{data.suppliers.map((supplier) => <div className="list-row" key={supplier.supplierId}><div><strong>{supplier.companyName}</strong><small>{supplier.contactName} · {supplier.email}</small></div><span>{supplier.rating.toFixed(1)} ★</span></div>)}</div>{user.role === "Admin" && <div className="panel"><p className="eyebrow">Admin tool</p><h2>Add supplier</h2><form action={createSupplierAction} className="form-grid"><label>Company<input name="companyName" required/></label><label>Contact<input name="contactName" required/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone" required/></label><label>Lead time (days)<input name="leadTimeDays" type="number" min="0" required/></label><label>Rating<input name="rating" type="number" min="0" max="5" step="0.1" required/></label><button className="button primary">Add supplier</button></form></div>}</section></Shell>; }


# Design

Pure dashboard functions calculate current values and classify risk at exact threshold boundaries. The shared server-rendered shell owns authenticated navigation and displays text labels alongside color styling.

## Verification

Dedicated dashboard tests cover values, excluded statuses, boundary labels, CSS mappings, and warning membership. A shell rendering test proves every primary destination and logout affordance.
